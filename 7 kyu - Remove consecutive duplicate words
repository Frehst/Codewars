def remove_consecutive_duplicates(s):
    s = s.split(' ')
    l = []
    for a in range (0,  len(s)-1):
        if s[a] != s[a+1]:
            l.append(s[a])
    l.append(s[-1])
    return ' '.join(l)
